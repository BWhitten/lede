language: c
branches:
    only:
        - travis
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y subversion g++ zlib1g-dev build-essential git python rsync man-db
    - sudo apt-get install -y libncurses5-dev gawk gettext unzip file libssl-dev wget
